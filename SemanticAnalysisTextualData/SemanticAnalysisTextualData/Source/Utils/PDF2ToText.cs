class Program
{
    /*  static void Main()
      {
          string pdfPath = "sample.pdf"; // Path to your PDF file
          string outputFolder = "ExtractedText";
          string outputFile = Path.Combine(outputFolder, "output.txt");

          // Ensure the output folder exists
          Directory.CreateDirectory(outputFolder);

          try
          {
              using (PdfDocument document = PdfDocument.Open(pdfPath))
              {
                  using (StreamWriter writer = new StreamWriter(outputFile))
                  {
                      foreach (var page in document.GetPages())
                      {
                          writer.WriteLine(page.Text);
                          writer.WriteLine("----- Page Break -----");
                      }
                  }
              }
              Console.WriteLine("Text extracted and saved successfully!");
          }
          catch (Exception ex)
          {
              Console.WriteLine("Error: " + ex.Message);
          }
      }*/
}
